import {eliaAPI} from "./axiosSettings/axiosSettings";
import {now} from "moment";

const functions={
    // getAllMyPrivateContent,
    // getAllMyPublicContent,
    // getAllMyCoCreatedContent,
    // getContent,
};

export default functions;